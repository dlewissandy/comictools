# The house template

What every newly founded publishing house starts with: the studio's
default styles (definitions only — example art is rendered on demand),
prompts, and references.  `storage/registry.py` copies this into a new
house's repo at founding when no machine template or sister house is
available.
